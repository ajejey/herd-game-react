# ads.txt — herdgamesonline.com
#
# Programmatic advertising on this site is managed exclusively by
# Journey by Mediavine.
#
# There are deliberately NO authorised-seller lines below yet. Journey
# requires exclusivity, and adding third-party seller entries here would
# breach it — Mediavine supplies and manages the seller lines themselves once
# a site is approved. If you are adding another ad partner, that is a decision
# to make before pasting anything into this file.
#
# Why the file exists at all while it is otherwise empty: without it, the
# single-page-app catch-all answered /ads.txt with the React shell as
# text/html, and a crawler reading HTML here treats the file as invalid rather
# than absent. An empty-but-valid text/plain file is the correct state.

contact=ajejey@gmail.com
